# Dependencies
node_modules

# Build / output
dist
dist-ssr
release
out
build
build-out-*
*.tsbuildinfo

# Vite (root cache; prebundled deps under node_modules/ are ignored via node_modules/)
.vite

# Local env (keep .env.example tracked)
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Test / coverage
coverage
.nyc_output

# Caches
.cache
.eslintcache
.turbo
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db
desktop.ini

# Electron — signing keys / certs
*.pem
*.p12
*.pfx

# electron-builder sometimes writes these at the package root (not only under release/)
builder-debug.yml
builder-effective-config.yaml
latest.yml
latest-mac.yml
latest-linux.yml
*.blockmap

# Bundle analysis / misc generated
stats.html
